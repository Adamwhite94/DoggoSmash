import styled from "styled-components";

const EndScreenContainer = styled.div``;
const EndScreenElements = styled.div``;
const EndScreenTitle = styled.h1``;
const EndScreenStatsText = styled.h3``;
const EndScreenButton = styled.button``;


export{
    EndScreenContainer,
    EndScreenElements,
    EndScreenStatsText,
    EndScreenTitle,
    EndScreenButton
}